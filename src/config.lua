local config = {
  enabled = true;
  disable_blur = true;
  disable_bloom = true;
}

local configDesc = {
  disable_blur = "Disables all instances of radial blur effects";
  disable_bloom = "Disables all instances bloom application";
}

return config, configDesc